POSTGRES_USER=tesco
POSTGRES_PASSWORD=
POSTGRES_DB=tesco

PORT=3002

DATABASE_URL="DATABASE_URL=postgresql://tesco:<password>@postgres:5432/tesco


# JWT Secret
# Use a strong random string (e.g., generate with: openssl rand -base64 32)
JWT_SECRET=your-super-secret-jwt-key-here

#Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your-32-character-encryption-key

# Google OAuth
# The callback URL where Google redirects after authentication
# Development: http://localhost:3002/auth/google/callback
# Production: https://api.yourdomain.com/auth/google/callback
REDIRECT_URI=http://localhost:3002/auth/google/callback

# Frontend
# The frontend URL where users are redirected after successful login
# Development: http://localhost:3000/dashboard
# Production: https://yourfrontend.com/dashboard
FRONTEND_REDIRECT=http://localhost:3000/dashboard
